# Backend — COMPLETADO

Todas las rutas y funcionalidades documentadas en este archivo fueron implementadas.

## Resumen

- ✅ `preciosInsumo.js` — CRUD de precios de insumos (ruta `/api/precios-insumo`)
- ✅ `recetas.js` — CRUD de recetas/BOM (ruta `/api/recetas`)
- ✅ `reportes.js` — KPIs, alertas, ventas-por-mes (rutas `/api/kpis`, `/api/alertas`, `/api/ventas-por-mes`)
- ✅ `consultasIa.js` — Historial de consultas IA (ruta `/api/consultas-ia`)
- ✅ `costeo.js` — Costeo dinámico por producto (ruta `/api/costeo/producto/:id`)
- ✅ `exportar.js` — Exportación CSV (rutas `/api/exportar/ventas.csv`, `/api/exportar/compras.csv`)
- ✅ Alias `/api/asistente` apuntando a `/api/consulta`
- ✅ Tabla `recetas` y `historial_consultas` en Schema.sql
- ✅ Dependencia `json2csv` en package.json

## Endpoints Implementados (22 en total)

Ver `backend/README.md` para la lista completa.
